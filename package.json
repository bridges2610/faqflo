{
  "name": "faqflo",
  "version": "2.0.0",
  "private": true,
  "description": "FaqFlo — get your business found by AI",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "_comment_typecheck": "--incremental false is not a performance choice. tsconfig sets incremental:true for the editor, and a stale tsconfig.tsbuildinfo once let this command exit 0 while six call sites had the wrong arity. A gate that can silently pass is worse than no gate.",
    "typecheck": "tsc --noEmit --incremental false",
    "check": "npm run typecheck && node scripts/check-use-server.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "@supabase/ssr": "^0.12.4",
    "@supabase/supabase-js": "^2.112.2",
    "next": "^16.3.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "server-only": "^0.0.1",
    "stripe": "^22.4.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2"
  }
}
